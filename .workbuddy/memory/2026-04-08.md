# 2026-04-08

## git-workflows 提交与推送

- 将 `skills/git-workflows/*` 的规则优化单独提交为 `3d94a655`：`feat(git-workflows): 🧭 harden release and push safeguards`
- 分支 `feature/issue-auto-processor` 已推送到 `origin` 并建立 upstream
- 本次按最小提交原则保留 `.workbuddy/`、release note/result 和过程性草稿文件在工作区，未一并提交

## AGENTS.md 重构

- 将根目录 `AGENTS.md` 重写为英文版精简 operating rules，去掉重复和大段场景化流程
- 收敛为 core operating rules：general workflow、spec gate、repository conventions、CloudBase cloud API guardrails、testing、release、skill routing
- 强化 CloudBase 云 API 约束：封装或调用前必须先读官方文档，优先核对 API Overview / 依赖资源 API / Manager SDK，并要求补测试覆盖 action 名、参数映射、参数大小写与 request shape

## skill 引用问题核查

- 确认 `cloudbase-skills` 外部仓库中的 `skills/cloudbase/references/ai-model-nodejs` 当前是有效目录，页面可访问且包含 `SKILL.md`
- 当前仓库中该路径不是源码路径；源码主入口仍是 `config/source/skills/ai-model-nodejs/SKILL.md`，`references/ai-model-nodejs` 属于 all-in-one / skills repo 构建产物路径
- 相关历史改动至少包括 `d58f85d2`（`fix: fix broken skill references and simplify allinone build script`）和 `d669275b`（`feat(skills): 🧭 strengthen cloudbase activation routing`），说明 skill 引用稳定性问题确实处理过

## 周期性 review 方案摸底

- 盘点了现有 review 资产：代码质量已有 `skills/codebase-audit`、PR 健康已有 `skills/pr-review-fix`、skill 质量已有 `skills/skill-authoring`，API/interface 正确性校验主要散落在 `tests/` 和 `config/source/skills/*/checklist.md`
- 文档新鲜度链路相对最弱：有 `scripts/check-readme-sync.js` 和 `crawl-docs.yml`，但发现 `docs/README-SYNC.md`、`scripts/crawl-tcb-docs.ts`、`scripts/generate-actionlist.ts` 等引用缺失，且 `nightly-build.yaml` 名称与触发方式不一致
- 初步建议把周期性 review 拆成 4+1 个 skill：code quality、skill quality、doc freshness、API/interface validation，再加一个 review automation orchestrator 负责 schedule、汇总和 issue/report 输出

## 周期性 review 设计收敛

- 用户确认采用 hybrid 输出，但希望进一步把高价值、可直接修复的问题推进到 PR，而不仅是 issue
- 复核后确认 CloudBase skill review 已以专门子流程存在于 `skills/skill-authoring` 中，核心入口为 `skills/skill-authoring/references/cloudbase-skill-review.md`，无需再重复造一个平级顶层 skill
- 因此新的拆分建议应调整为 3+1：code/API review、doc freshness review、review automation orchestrator，再复用现有 `skill-authoring` 中的 CloudBase skill review 能力

## review skills 落地

- 新增 `skills/api-contract-review/`，聚焦 CloudBase 云 API 契约审查，强制先读 API Overview / 依赖资源 API / Manager SDK 文档，并要求契约修复必须补 targeted tests
- 新增 `skills/doc-freshness-review/`，覆盖 `doc/` 与 `README.md` / `README-EN.md` / `README-ZH.md` / `mcp/README.md` 的文档漂移审查与 report / issue / PR 升级
- 新增 `skills/review-automation-orchestrator/`，负责周期性 review 的 reviewer 分发、聚合与 escalation，明确复用 `codebase-audit`、`pr-review-fix` 和 `skill-authoring`
- 新增 `tests/review-skills.test.js`，先让三类 skill 缺失场景失败，再补齐实现并验证 3 个测试全部通过

## local skill 安装准备

- 使用 `manage-local-skills` 流程校验了 `skills/api-contract-review`、`skills/doc-freshness-review`、`skills/review-automation-orchestrator`，结构校验已通过
- 针对 project scope + symlink 模式做了安装前检查：当前 `.agents/skills`、`.codebuddy/skills`、`.claude/skills` 下均未发现这 3 个 skill 的现有安装冲突
- 下一步待用户确认后，可将 `skills/` 作为唯一维护源，通过 `.agents/skills` canonical symlink 和 agent-specific symlink 挂载到 CodeBuddy 与 Claude

## local skill 安装完成

- 已将 `api-contract-review`、`doc-freshness-review`、`review-automation-orchestrator` 以 project scope + symlink 模式安装到 CodeBuddy 与 Claude
- 最终形成 9 个入口：3 个 `.agents/skills/*` canonical symlink 指向 `skills/*` 源目录，6 个 `.codebuddy/skills/*` 与 `.claude/skills/*` 再指向 canonical 入口
- `git status --short` 显示这 9 个 symlink 为未跟踪项；如果后续希望把安装入口也纳入 PR，可直接加入提交
