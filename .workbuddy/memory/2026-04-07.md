# 2026-04-07

## 优化 NoSQL 工具的索引能力可发现性

- 调整 `mcp/src/tools/databaseNoSQL.ts` 中 `readNoSqlDatabaseStructure` / `writeNoSqlDatabaseStructure` 的 description 与 schema 字段说明，显式写出索引查询、添加索引、删除索引入口
- 新增 `mcp/src/tools/databaseNoSQL.test.ts` 回归测试，确保 NoSQL 结构工具的元数据持续暴露索引管理入口
- 已重新生成 `scripts/tools.json` 与 `doc/mcp-tools.md`，让对外镜像文档同步包含 `updateOptions.CreateIndexes` / `DropIndexes` 的说明

## 提交与 PR

- 将 NoSQL 索引入口描述优化从已有的 `fix/mysql-not-found-hint` 分支拆出，单独提交到 `fix/nosql-index-tool-discoverability`
- 提交 commit `49768652`：`fix(database): 🧭 clarify NoSQL index management entry points`
- 已创建 PR #481：`fix(database): 🧭 clarify NoSQL index management entry points`
- 创建 PR 后检查初始状态：CodeQL 和 Sync to CNB 已通过，`build-and-publish` 仍在进行中，暂无 review 评论
